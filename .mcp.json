{
  "mcpServers": {
    "agent-middleware": {
      "type": "http",
      "url": "${AGENT_MIDDLEWARE_MCP_URL}",
      "headers": {
        "X-API-Key": "${AGENT_MIDDLEWARE_API_KEY:-}"
      }
    }
  }
}
