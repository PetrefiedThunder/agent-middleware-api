# Agent-Native Middleware API - Dependencies
# Python 3.11+

# --- Core Framework ---
fastapi>=0.139.0
uvicorn[standard]>=0.49.0
pydantic>=2.13.4
pydantic-settings>=2.14.2
email-validator>=2.3.0  # Keep FastAPI/Pydantic OpenAPI email schema stable in CI
structlog>=24.1.0       # Structured startup/runtime logging

# --- IoT Protocol Bridge ---
aiomqtt>=2.5.1          # Async MQTT client
aiocoap>=0.4.17          # CoAP protocol support

# --- Telemetry / Autonomous PM ---
httpx>=0.28.1           # Async HTTP client (git API calls + test client)

# --- Media Engine ---
python-multipart>=0.0.32 # File upload support (>=0.0.30 fixes CVE-2026-53538/53539, >=0.0.31 fixes CVE-2026-53540)

# --- Utilities ---
python-dotenv>=1.2.2    # .env file loading
cryptography>=49.0.0    # Ed25519 trust-plane signatures (>=48.0.1 fixes GHSA-537c-gmf6-5ccf)
redis>=8.0.1            # Redis-backed rate limiting and runtime persistence
asyncpg>=0.31.0         # PostgreSQL-backed runtime persistence
greenlet>=3.5.3         # SQLAlchemy async engine support
stripe>=15.3.1           # Stripe payment processing
mcp>=1.28.0,<2         # MCP (Model Context Protocol) SDK for tool servers (>=1.28.0 carries pyjwt>=2.13.0 fixing PYSEC-2026-175..179 and starlette>=1.3.1 fixing CVE-2026-48817/48818/54282/54283 + PYSEC-2026-161); upper bound guards against breaking 2.x

# --- Database ---
sqlmodel>=0.0.39        # SQLAlchemy + Pydantic hybrid ORM
alembic>=1.18.5        # Database migrations
aiosqlite>=0.22.1      # Async SQLite driver for testing

# --- Development ---
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
mypy>=2.2.0            # Type checking
