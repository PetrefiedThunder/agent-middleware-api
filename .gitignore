# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
.venv/
env/

# uv — requirements.txt is the single source of truth (see CONTRIBUTING).
# uv may regenerate uv.lock locally, but it must not be committed or it drifts.
uv.lock

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment & Secrets
.env
.env.local
.env.*.local
*.pem
*.key

# Testing
.pytest_cache/
htmlcov/
.coverage
coverage.xml
coverage.json
test.db
*.db
*.db-shm
*.db-wal

# Local dogfood / demo throwaway state
data/dogfood_partner_notes.jsonl
data/dogfood_trust_plane.db*
data/demo_trust_plane.db*
data/quickstart/
data/live-loop-proof/
# live_loop_proof.py --output-dir target used for the 2026-08-25 rerun
data/validation-run/

# OS
.DS_Store
Thumbs.db

# Docker
*.log
.vercel/

# Agent tooling scratch: throwaway git worktrees used to run checks in
# isolation. Full repo copies (including .env.production and coverage
# artifacts) — never commit these.
.claude/worktrees/
